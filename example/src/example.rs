use std::io::{self, Write};

 fn Array_Example() {
    let mut Num1: i32;
    let mut Num2: i32;
    let mut A: i32 = 12;
    let mut B: i32 = 34;
    type Int_Array = [i32; 5 - 1 + 1];
    type Int_Array2 = [i32; 5 - 1 + 1];
    let Arr1: Int_Array = [1, 2, 3, 4, 5];

    print!("{}", "Array Example");
 }
